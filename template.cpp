#include <iostream>
#include <fstream>
using namespace std;

int main()
{
    string line;
    ifstream file("input");
    while (getline(file, line))
    {
        ;
    }
    file.close();
    return 0;
}